import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // The little Next.js mascot in the corner of the dev server. Off so the
  // demo UI looks the same in dev as it does in a production build.
  devIndicators: false,
};

export default nextConfig;
